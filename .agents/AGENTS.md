# Project Rules

- **行程表編輯、網頁轉換與 Git 推送規則**：
  - 預設專注修改與維護 Markdown 行程表（僅維護 `README.md`，**不需要同步修改 `2026東京親子自由行_V10_Henna.md`**）。
  - **嚴禁在每次微調修改 Markdown 後自動執行網頁版轉換（`build_pwa.py`）或自動 `git push`**。
  - 只有在使用者明確要求（例如：「轉成網頁版」、「更新 PWA」、「幫我 push」）時，才執行網頁建置與代碼推送。

- **導航連結唯一真相來源規則（2026-08 起，Claude Code 與 Antigravity 共同適用）**：
  - `places.json` 是**所有 Google Maps 導航連結的唯一真相來源**。
  - **嚴禁直接編輯以下四個衍生檔**，它們由 `python3 sync_places.py --generate` 自動生成，手動修改會在下次生成時被覆蓋：
    - `navigation_links_dict.json`
    - `first_destinations.json`
    - `text_entities.json`
    - `navigation_links.html`
  - 要新增或修正地點：**只改 `places.json`**，接著依序執行
    `python3 sync_places.py --generate` ➔ `python3 sync_places.py --fix`（同步 README 內文網址）➔ `python3 build_pwa.py`（僅在使用者要求更新 PWA 時）。
  - 若直接在 `README.md` 內文新增了地點連結，執行 **`python3 sync_places.py --adopt`** 把這些「孤兒連結」收編進 `places.json`。
    ⚠️ 孤兒連結是最危險的一類：它們不在任何字典檔中，稽核規則因找不到同伴而抓不到，`--check` 也會略過。2026-08 抽查 26 個孤兒連結，其中 11 個 Place ID 是無效或指向錯誤地點的。
  - `canonical_nav_map.json` 為歷史遺留檔，**已無任何程式讀取**，請勿再寫入或依賴它。

- **Place ID 品質規則（重要，過去曾大量出錯）**：
  - **嚴禁憑空編造 Place ID 或座標。** 曾發生以尾碼遞增（如 `…F88`、`…F90`、`…F91`）偽造 ID，以及把同一經度複製給多家店的情形，導致導航靜默失效。
  - Place ID **必須經實際查證**取得，可用 [Place ID Finder](https://developers.google.com/maps/documentation/javascript/examples/places-placeid-finder) 或具備 API key 的 Google Maps 工具（Antigravity 的 google-maps MCP 可直接使用）。
  - ⚠️ **HTTP 200 不代表 Place ID 有效**：Google 對任何 `query_place_id` 都回 200，然後靜默退回用座標定位。要驗證有效性請用 Places API 的 Place Details，或執行離線稽核 `python3 place_id_audit.py`。
  - 每次改動導航連結後，務必執行 `python3 place_id_audit.py`（或包含它的 `full_validation_pipeline.py`），確認 5 項偽造偵測規則全數通過。

- **查詢 Place ID 只能用 Places API，嚴禁用 Geocoding（2026-08 實際踩過的坑）**：
  - **正確工具**（google-maps MCP）：
    | 用途 | 工具 |
    | :-- | :-- |
    | 由名稱／店名找地點 | **`maps_search_places`**（Places Text Search） |
    | 由既有 place_id 取權威資料 | **`maps_place_details`**（Place Details） |
    | 由純地址找座標 | `maps_geocode`（**僅此情境**適用） |
  - ⚠️ **前置條件：Google Cloud Console 必須同時啟用「Places API（新版）」與「Places API (Legacy)」。**
    上述兩個 MCP 工具走的是 **Legacy** 端點；若只啟用新版，呼叫會失敗。
  - 🚨 **工具失敗時，嚴禁靜默改用 `maps_geocode` 頂替**——這正是 2026-08 出錯的真正原因：
    當時 Legacy Places API 未啟用，導致 fallback 到 Geocoding，而 **Geocoding 回傳的是「地址／行政區」
    不是「商家 POI」**，遇到店名與地名同名時會**靜默降級**，回傳一個真實存在、格式正確、座標也在日本的 ID
    —— 但那是**町名的 ID**。實例：查「変なホテル東京 浅草橋」得到町名「淺草橋」；查「雷門」得到台東區
    的町名「雷門」；查「台場駅」得到町名「台場」；查「井の頭池」降級成「井の頭恩賜公園」。
  - **若 Places 工具不可用，正確做法是停下來回報「工具不可用、需啟用 Legacy Places API」，
    而不是換一個語意不同的 API 繼續跑完並宣稱成功。**
  - **每筆結果都要自檢是否為 POI**，符合任一條即為降級、必須丟棄重查：
    `formattedAddress` 無番地門牌｜`types` 含 `political`／`sublocality`／`locality`／`administrative_area_*`｜
    回傳名稱比查詢字串更短更泛化｜查車站卻沒回「駅／Station」｜查店家卻回建物名或地名。
  - ⚠️ 東京有大量町名與地標同名（**雷門、淺草橋、台場、銀座、築地、押上、有明**），查這些務必用完整設施全名。
  - ⚠️ 小地物易被降級到父層（井の頭池⊂井の頭恩賜公園、南展望室⊂東京都庁、世界市集⊂迪士尼樂園），
    請確認回傳名稱就是目標層級，不是它的容器。
  - **座標與 place_id 必須同一次查詢取得**，不可只更新其中一個（同一 place_id 只有一個標準座標）。
  - 查不到就在報告中標「查無」，**嚴禁用地區級 ID 頂替**——指向町名的導航連結比沒有連結更危險。

- **`verified_*` 佐證欄位與 `poi_level` 宣告規範（2026-08-14 新增）**：
  - `places.json` 每筆須記錄 **`verified_name`** 與 **`verified_address`**，兩者**必須是 Places API 原封不動的回傳值**。
    它們的用途是讓日後能**離線**判斷有沒有被降級，不必再消耗 API 額度。
  - 🚨 **嚴禁手寫或編造這兩個欄位**。曾發生為了讓 R6 通過而把 `verified_name` 寫成
    `"Tokyo Disneyland Partners Statue Fallback"`（`Fallback` 不可能是 API 回傳值）的情形——
    **這等於把稽核機制本身廢掉**。`place_id_audit.py` 的 R7 會偵測此類痕跡。
  - **有些地點在 Google 上確實沒有獨立 POI**（園區內的雕像與遊行、航廈裡的餐廳街、公園裡的廣場、
    商場中未單獨登錄的櫃位）。這**不是造假的理由**，正確做法是誠實宣告：

    ```json
    "夥伴雕像 (パートナーズ像)": {
      "url": "https://www.google.com/maps/search/?api=1&query=35.0000000,139.0000000&query_place_id=ChIJxxxxxxxx",
      "poi_level": "container",
      "container_note": "Google 無獨立 POI，導航指向東京迪士尼樂園園區",
      "verified_name": "API 回傳的容器名稱（原封不動）",
      "verified_address": "API 回傳的容器地址（原封不動）",
      "verified_at": "2026-01-15"
    }
    ```

  - `poi_level` 取值：**`"exact"`**（預設，可省略；指向該地點自身的 POI）／
    **`"container"`**（只能導到所在建物、園區或公園層級）。
  - 宣告 `container` 者**豁免 R6 的番地檢查**，但 `verified_name`／`verified_address` **仍必須是真實回傳值**。
  - 每次查證後請在報告中**列出所有 `container` 條目**，讓使用者知道哪些導航只到建物層級。
  - ⚠️ **驗證管線 PASS 不等於 Place ID 正確**：HTTP 200 測試與 5 條離線規則都抓不到「降級到行政區」
    （那些 ID 真實存在、格式正確、座標也在日本），**不可拿管線通過當作查證正確的證據**。

- **PWA 卡片「📍 導航」按鈕指向規則（2026-08-14 修訂，舊版寫反過，請重讀）**：
  - 卡片右上角那顆**唯一**的導航按鈕，指向**站在該時段起點時，第一個要用雙腳走到的地點**。
    判斷問句是「**下一步往哪走？**」，**不是**「這段的目的地是哪裡？」。
  - **交通時段幾乎一律指向「上車的起站」**：`Day 3 前往東京車站` → **淺草橋站**（不是東京車站）；
    `Day 4 返回淺草橋` → **吉祥寺站**（不是飯店）；`Day 2 返回淺草橋（親子組）` → **迪士尼東巴士總站 1 號站牌**。
  - 🚨 舊版 skill 曾寫「導向第一個要**抵達**的目的地」並註明「嚴禁誤導向出發點」，範例也全給終點——
    **那是錯的**，2026-08-14 已全面修正，共修 21 張卡片。完整說明見
    `.agents/skills/generate-travel-itinerary/SKILL.md` 的「下一步走去哪原則」。
  - 內文超連結不受此限：起站與終點站**兩者都要**帶導航連結，按鈕只給「下一步」。
  - 實作只改 `places.json` 的 `first_dest`（key＝`"{天數}_{標題關鍵字}"`，須為標題子字串且同天不重複命中），
    再跑 `python3 sync_places.py --generate`。**兩張卡標題相同時，先在 README 改標題使其可區分。**

- **🔒 個資與私人設定規則（2026-08-14 新增，本 repo 為 public）**：
  - `README.md`、`itinerary.html`、`CLAUDE.md`、`.agents/` 底下所有檔案（含本檔與兩份 skill）
    **全部會被公開到 GitHub 與 GitHub Pages**。把個資「搬到 skill」並不會讓它變私密。
  - **排行程前先讀 `.agents/private/profile.local.md`**（已被 `.gitignore` 排除，僅存在於本機）。
    該檔記錄：住家所在地與車程、成員精確年齡與身高、長輩免費資格、訂位與票券編號、健康註記。
    若檔案不存在（他人 clone 本 repo），依 skill 的通則處理即可，不要臆造。
  - **嚴禁寫進任何公開檔案**：住家縣市／地址、成員精確年齡與身高、訂位或取票編號、
    電話、email、護照號碼、信用卡與銀行資訊。
  - 公開版的寫法：住家一律只寫「住家」；小孩寫「小學生」或「皆已超過 ○○ cm 門檻」；
    長輩寫「符合免費資格，出示護照即可」，**不直接斷言年齡**。
  - 刻意保留公開的項目：**住宿飯店名稱與地址**、**航班班號與時刻**——PWA 導航與行前提醒需要，
    且兩者本為公開可查資訊。此為權衡後的決定，不是遺漏。
  - ⚠️ 既有 git 歷史仍含有先前寫入的個資；要徹底移除需重寫歷史並強制推送，屬不可逆操作，
    **必須經使用者明確同意才執行**。

- **行程表文字風格：不加主觀形容詞、不寫贅字（2026-08-14 新增）**：
  - 行程表是**現場照著做的操作手冊**，不是旅遊型錄。判準只有一句：
    **「刪掉這幾個字，讀者的行動會不會改變？」不會改變就刪掉。**
  - **禁止**：主觀形容詞與情緒詞（熱騰騰、璀璨、極為震撼、超人氣、必瘋狂）、句尾驚嘆號、
    語氣強化詞（完全、絕對、極速、頂級）、自我推銷式評語（最省力、CP 值極高、長輩小孩皆愛），
    以及可由上下文推得的贅述（例如在雨天方案裡再寫「雨天室內最佳去處」）。
  - **必留**：時間、價格、車站編號、樓層、營業時間、公休日、⚠️🚨 安全警語、店名與導航連結、
    `票務狀態` 標記、判斷規則中的數字（如「排隊 ≤ 3 組」）。
  - ⚠️ **這類文字調整一律先把「改前／改後」對照送使用者確認，同意後才動 `README.md`**；
    改完要比對導航連結總數與價格／站號／票務標記數量皆未減少，並確認 PWA 卡片摘要沒有變空白。
  - 完整規範與改寫示範見 `.agents/skills/travel-itinerary-auditor/SKILL.md` 的
    「行程表內文敘述精簡規範」。

- **PWA 卡片是「兩層資訊架構」，兩層各有硬性長度上限（2026-08-17 新增）**：
  - **第一層＝卡片摘要**（`card-summary`，沒點開就強制看到）：**目標 ≤ 80 字、破格線 120 字**。
    只回答「這段去哪、花多少錢」；推薦菜色、備案餐廳、點餐細節一律進第二層。
  - **第二層＝「📖 完整說明」抽屜**（`original-content-holder`）：手機上抽屜可視高度為 **650px**，
    **展開後不宜超過 2 屏（1300px）**。超過就用 `<details>` 分層，**不是刪字**。
  - 🔧 **改摘要只改 `build_pwa.py` 的 `CUSTOM_SUMMARIES_V10`，嚴禁為了縮短摘要去刪 `README.md` 內文**。
    該 dict 正是為覆蓋 PWA 摘要而存在；README 由 Docsify 直接渲染，必須維持完整。
  - `build_pwa.py` 的 **`warn_long_summaries()`** 會在每次編譯後分兩級回報（>120 字逐張點名、
    80～120 字只報數量），並偵測**空白摘要**。**編譯後務必看這段輸出**，不要只看 `✅ Successfully built`。
  - ⚠️ **空白摘要＝紅燈**：代表對應時段的 README 內文被刪過頭，`clean_markdown_for_summary()`
    取不到首句。看到 🚨 一定要回頭修，不可放行。

- **抽屜內容分層規則：判準是「打開抽屜的當下，人在現場」（2026-08-17 新增）**：

  | 一律攤開 | 一律收進 `<details>` |
  | :-- | :-- |
  | 現在往哪走、搭什麼線、幾站、哪個出口 | 行前必讀介紹文連結 |
  | ⚠️🚨 安全與避坑警語、🚻 廁所位置 | 票券規則、卡種比較、背景知識 |
  | 價格、營業時間、樓層、`票務狀態` 標記 | 「如果搭錯方向怎麼辦」的防呆對照 |
  | 站內指標實景照片（要照著找看板） | 多情境分支中**不屬於你**的那幾種 |

  - **多情境分支**（「若在 A 用餐…若在 B 用餐…若在 C 逛街…」）必須各包一個 `<details>`，
    `<summary>` 寫情境名。現場只會用到一種，其餘全是干擾。
  - 🚨 **`<details>` 嚴禁放在時段的第一行**：`clean_markdown_for_summary()` 會把標籤本身當成首句
    寫進摘要，在 `itinerary.html` 產生**沒有結尾的孤兒 `<details>`**（2026-08-17 於「抵達羽田機場
    第 3 航廈」實際踩到，58 open / 57 close）。該函式已加防護會跳過收合區塊，但仍請把
    「現場第一步做什麼」留在最前面。
  - ✅ **改完必查**：`<details>` 與 `</details>` 在 `README.md` 與 `itinerary.html` **兩邊都要配對**，
    且要**逐張卡片**檢查——整份檔案總數相等，仍可能有單張卡片不配對。
  - 💡 巢狀 `<details>` 是合法的（例如整段「☔ 雨天備案」本身就是一個大摺疊，裡面再放小摺疊），
    檢查配對時不要把巢狀誤判成錯誤。

- **🚻 是「戶外久待時段的廁所指引」專用標記，不得挪作他用（2026-08-17 新增）**：
  - `build_pwa.py` 以**內容偵測**（時段內文是否含 `🚻`）決定卡片要不要長出「🚻 廁所」標籤。
    因此 **🚻 在 `README.md` 各時段內文中是保留字元**，寫在別處會誤觸發標籤。
  - 2026-08-17 實際踩到：Day 1「Step 4 辦卡後整備」與 Day 3「順便上洗手間」原本用 🚻 當裝飾，
    導致兩張室內卡片誤掛廁所標籤，已分別改為 🎒 與 💧。
  - 置頂頁（行前常識）不受此限——置頂頁走另一條渲染路徑，不產生時段標籤。

- **`first_dest` 必須明確指定，不可依賴 fallback（2026-08-17 新增）**：
  - `get_first_destination_map_link()` 的第三段 fallback 是 `navigation_links_dict.json` 的
    **最長字串匹配**，會挑「標題裡最長的那個地名」——那**不一定是第一個要走到的地點**。
  - 實例：Day 2「晨間不忍池與清水觀音堂散步」沒設 `first_dest`，fallback 挑中「清水觀音堂」（5 字）
    而非「不忍池」（3 字），導致導航指向該段的**終點**而非起點，違反「下一步走去哪原則」。
  - ✅ **凡是標題含兩個以上地名的時段，一律明確設 `first_dest`**，不要讓它去猜。
  - ⚠️ **在時段開頭新增任何含連結的行（例如 `📄 行前必讀介紹文`、`🚻 廁所`）之前，
    先確認該卡有沒有 `first_dest`**：沒有的話，第二段 fallback「內文第一個連結」會被新行搶走，
    導航按鈕會指到部落格文章。此坑 2026-08 已重複踩過兩次。

- **同一天出現兩張標題完全相同的卡片＝規格衝突，必須先改標題（2026-08-17 補充）**：
  - 受影響的**不只** `first_dest`，還有 **`CUSTOM_SUMMARIES_V10`**——它的 key 也是 `(天數, 標題)`，
    同名時改一張會兩張一起變。
  - ⚠️ **雙向 substring 會讓「短標題」被「長標題」的 key 命中**：`5_返回淺草橋` 這種 key
    會同時命中「返回淺草橋」「返回淺草橋飯店」等全部同字根的卡片，且 first match wins。
  - ✅ **命名慣例：同一天的重複時段一律用「（出發地）」或「（分組）」後綴區分**。
    已套用實例：Day 2 的「返回淺草橋（長輩組）／（親子組）」、
    Day 5 的「返回淺草橋（新宿出發）／（押上出發）／（台場出發）」（2026-08-17 修正，
    改前三張全含「返回淺草橋」字根、無法個別設定 `first_dest` 與摘要）。
  - 後綴要挑**現場用得到的資訊**（出發地、分組），不要用「（方案 A）」這種只有讀行程表時
    才有意義的代號——而且中文「方案」與解析器切分支用的英文關鍵字 `Plan A` 容易混淆。

- **取得 Place ID 的首選方法：Google Maps 搜尋結果的 `!19s` 參數（2026-08-17 新增）**：
  - 在 Google Maps 網頁版**搜尋列表頁**（多筆結果時），每個結果的 `<a href>` 內含
    `!19sChIJxxxxxxxx` —— **那就是 Google 自己回傳的 place_id，不是推算值**，可靠度最高。
  - 只有**單一結果**直接跳轉到 place 頁時才沒有 `!19s`，此時退回用 URL 裡的
    `!1s0x…:0x…`（ftid）換算，換算後**必須**用 `maps/place/?q=place_id:` 反查驗證。
  - 兩種方式取得後都要做 POI 自檢（見上方 Places API 規則），並在 `places.json` 的
    `verified_source` 誠實記錄**是哪一種方法、有沒有反查**。
  - ⚠️ 公廁這類 POI 的地址常要展開才看得到，用 `button[data-item-id="address"]` 的
    `aria-label` 取得的才是含番地的完整地址（`place_id_audit.py` 的 R6 需要）。

- **驗證管線偶發的假警報：先重測再判定（2026-08-17 新增）**：
  - 階段 3 的連結測試會受**暫時性 DNS 解析失敗**影響（`Name or service not known`），
    這不等於連結失效。**同一個連結請用 `curl` 連測 3 次**再下結論。
  - **403／429 是 Google 與部分媒體站的反爬回應，管線視為有效**，不要誤報成壞連結。

- **使用者貼來的外部攻略文一律逐項查證，不得照抄（2026-08-20 新增）**：
  - 網路攻略常把**別家連鎖店的規則**或**舊價目**套到這家，照抄會讓人在現場找不到那個功能。
  - 2026-08-20 實例（壽司郎秋葉原駅前店），一份指南四處有問題：

    | 原文 | 查證結果 |
    | :-- | :-- |
    | 盤色表 黃150／紅210／黑290／白370 | ❌ 官方菜單實際 ¥140～¥780 共 28 種價位，非四色分級；平板每項直接標價 |
    | 平板可勾「シャリ小さめ」醋飯減半 | ❌ 壽司郎沒有；它是「(ミニしゃり)」標註的品項。シャリハーフ 是藏壽司、半シャリ 是濱壽司 |
    | 報到掃 QR Code | ❌ 該預約只有「お客様番号」四位數字，畫面上根本沒有 QR |
    | 全程自助無人化結帳 | ⚠️ 官方只寫「会計方法は店舗によって異なる」，未載明本分店 |

  - **查證優先序**：該分店的官方店舖頁與官方菜單頁 ➔ 官方 FAQ ➔ 才輪到部落格。
  - **不確定的部分要明講**（「依店舖而異，現場為準」），不要為了讀起來順而寫死。
  - 💡 **順手撈到的期間限定往往比攻略本身有用**：查官方菜單時發現 8/19–8/30 檔期的
    本鮪中とろ ¥140（比基本盤 ¥150 還便宜），這比盤色對照表實用得多。

- **凡是要排隊的景點，先查官方標示的「常態等待時間」再回推抵達時間（2026-08-20 新增）**：
  - 🚨 2026-08-19 實際踩到：Day 1 Plan C 原排 18:10 抵達都廳看 18:25 日落，
    但官方頁明載「**入室まで20～30分お並びいただいております**」——常態就要排 20～30 分，
    18:10 到等於 18:30～18:40 才上得了 45 樓，**整個錯過夕陽**，該方案的賣點直接消失。
  - 更糟的是原本寫的中止規則是「排隊超過 20 分鐘就放棄」，而官方常態就是 20～30 分——
    **這條規則幾乎每次都會觸發，等於該方案永遠不會被執行**。
  - ✅ 正確做法：**抵達時間 ＝ 目標時刻 − 官方常態等待 − 安檢／換票時間**；
    放棄門檻要設在**明顯高於常態值**的地方（該例改為 45 分鐘）。

- **訂位／取號流程一律以使用者的實際訂位畫面為準（2026-08-20 新增）**：
  - 通用攻略寫的流程未必是這筆訂位拿到的形式。**請使用者截圖，看到再寫**。
  - 2026-08-20 實例：LINE 預約完成畫面給的是 **「お客様番号」四位數字**，不是 QR Code；
    且 **「予約日時」是一個 15 分鐘的時間帶**（17:30－17:45）不是準點——
    🔒 標記要跟著改成「時間帶內報到，逾時號碼會被釋出」。
  - **餐廳人數／座位問題要查桌型規格與配桌規則，不要從訂位欄位推論**。
    同日先踩過：看到「大人・子ども 3人／未就学児 2人」就推論「會配到 3 人座小桌」並寫成警語；
    查證後發現壽司郎的テーブル席是**固定規格、每邊長椅坐 3 人、上限 6 人**，
    且**每 6 人配一張桌**——申報 3 人或 5 人配到的是同一張，根本沒有小桌。
    真正的風險是被分到**カウンター（吧檯）席**，那才無法五人相連。
  - 🔒 **實際的訂位編號、取票號碼一律不得寫進 repo**（見本檔個資規則），
    只寫「在畫面哪個位置找得到」。

- **`CUSTOM_SUMMARIES_V10` 的條目「順序」會決定勝負（2026-08-18 新增）**：
  - 查表是 exact 優先、再**雙向 substring**、**first match wins**，比對依 dict 的插入順序。
  - 🚨 實例：新增 Day 1 Plan C 的「返回淺草橋（新宿出發）」後，該卡摘要被 Plan A 既有的
    「返回淺草橋」搶走，正面顯示成「步行至秋葉原站搭一站」——**完全錯誤的路線，且編譯不報錯**。
  - ✅ **新增較長、較具體的 key 時，必須把它排在既有短 key 之前**，並回測短 key 那張卡沒被反向污染。

- **「📄 行前必讀介紹文」放在時段第一行會污染自動摘要（2026-08-18 新增）**：
  - `clean_markdown_for_summary()` 取內文首句，於是整串文章標題被寫進卡片正面，
    使用者沒點開只看得到一排文章名，看不到「這段去哪、做什麼、花多少錢」。
  - 2026-08-18 一次掃出 **7 張**中招（Day2 DPA、Day4 前往吉祥寺與哈莫尼卡橫丁、
    Day5 淺草寺、Day6 築地與登機與免稅店）。
  - ✅ **凡是要在段首加介紹文的時段，同時補一條 `CUSTOM_SUMMARIES_V10`**。
    檢查方式：掃編譯後的卡片，摘要開頭是 `📄`／`🔗`／前 24 字含「介紹文」者應為 **0 張**。

- **雨天備案優先用 `<details>` 摺疊，不要輕易新增晴／雨分支（2026-08-20 新增）**：
  - 判準是**晴雨兩版是不是共用同一批店家**。若只差順序與停留時間，就該是同一張卡內的摺疊區；
    新增分支要同時改 `parse_v10_markdown()` 與 `render_full_pwa_html()`
    （`CLAUDE.md` 標示最脆弱處），維護面積加倍。
  - 2026-08-20 實例：Day 3 阿美橫丁的雨天版與晴天版走同樣五家店，只差「快掃 40 分」與
    「逛 80 分」，故做成卡內的「☔ 下大雨時的替代順序」摺疊區。
  - **值得新增分支的情況**：整段行程換到不同區域（如 Day 5 的台場雨天備案）。
